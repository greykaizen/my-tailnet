---
# Windows Users Role Variables
# These variables are loaded from vault and group_vars

# Default values (overridden by group_vars/windows_group.yml)
windows_team_group: "TeamShare"
windows_power_users_group: "Power Users"
windows_admin_group: "Administrators"
disable_guest_account: true

# User and service account data loaded from vars/vault.yml:
# - team_users (list of team member accounts)
# - service_accounts.ansible_admin
# - service_accounts.smbmount
